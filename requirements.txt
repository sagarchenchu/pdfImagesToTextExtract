# ── Core ML / OCR stack ────────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.37.0
sentencepiece>=0.1.99  # required by transformers tokenizer backend (TrOCR)
tokenizers>=0.15.0     # fast tokenizer for RoBERTa/TrOCR decoder
easyocr>=1.7.1
timm>=1.0.0            # required by EasyOCR/CRAFT detector; 1.x keeps timm.models.layers compat shim

# ── EasyOCR runtime dependencies (transitive, but listed explicitly) ────────
scipy>=1.9.0           # used by scikit-learn and easyocr internals
scikit-learn>=1.2.0    # KMeans and clustering used by easyocr
PyYAML>=6.0            # easyocr config parsing
Shapely>=1.8.0         # polygon operations in easyocr text detection
pyclipper>=1.3.0       # polygon clipping used by easyocr
python-bidi>=0.4.2     # RTL text direction support in easyocr
six>=1.16.0            # compatibility shim used by python-bidi and others

# ── PDF support ────────────────────────────────────────────────────────────
PyMuPDF>=1.23.0        # imported as `fitz`

# ── Image handling ─────────────────────────────────────────────────────────
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0  # EasyOCR uses cv2; headless avoids extra Qt DLLs

# ── Packaging helper (not needed at runtime) ───────────────────────────────
# pyinstaller>=6.3.0   # install separately: pip install pyinstaller
